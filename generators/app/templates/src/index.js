// @flow

/**
 * Returns the sum of a and b
 *
 * @param {Number} a
 * @param {Number} b
 * @returns {Number} Sum of a and b
 */
function sum(a: number, b: number): number {
  return a + b;
}

module.exports = sum;
